name: feature-flag-cleanup
description: Clean up unused or expired feature flags in the current repository via the GitHub CLI.
usage: gh feature-flag-cleanup <feature-flag>
command: ./gh-feature-flag-cleanup
