name: feature-flag-cleanup
description: Clean up unused or expired feature flags in any codebase via the GitHub CLI.
usage: gh feature-flag-cleanup <repository> <feature-flag>
command: ./gh-feature-flag-cleanup

